var template_builder = Class.extend({
    init: function (opt) {
        $.extend(this, opt);
        // this.uniqueId();
        // this.make_main_template_structure();
        // this.define_tabs();
        // this.build_tabs();
        // this.build_or_get();
        // this.make_dragabble_sections();
        // this.make();
    },

});